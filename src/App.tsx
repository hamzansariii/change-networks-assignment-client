import { BrowserRouter } from "react-router-dom";
import Router from "./routes/main";

function App() {
  return (
    <BrowserRouter>
      <Router />
    </BrowserRouter>
  );
}

export default App;
